version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8081:8081"
    env_file:
      - .env

  frontend:
    build: ./frontend
    ports:
      - "8501:8501"
    depends_on:
      - backend
    env_file:
      - .env